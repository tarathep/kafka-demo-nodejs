{
  "name": "kafka-demo-nodejs",
  "version": "1.0.0",
  "description": "example kafka producer and consumer",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tarathep",
  "license": "ISC",
  "dependencies": {
    "kafka-node": "^5.0.0"
  }
}
